<h1> myRPC-client c-file

    Программа для реализации клиент-серверного,подключения с целью удаленного логирования
    В файле клиента используются параметры -c, -h, -p, -s, -d, --help.
    Открывается сокет, и подключается к серверу, для последующего отправлерния запросов
    